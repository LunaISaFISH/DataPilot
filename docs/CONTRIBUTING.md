# Contributing

This time-boxed P0 develops directly on `main` with small, single-purpose commits.

Before each milestone, run the relevant focused tests. Before delivery, run `make test`.
Never commit secrets, real sensitive data, generated virtual environments, runtime artifacts,
or unreviewed model payloads. Commit identity must come from confirmed local human Git
configuration.

