export const meta = {
  name: 'datapilot-backend-v2',
  description: 'Build the DataPilot v0.2 backend against docs/BUILD-SPEC.md: contracts, engine, AI layer, samples, API, integration, adversarial review',
  phases: [
    { title: 'Foundation', detail: 'models, contract v2, storage' },
    { title: 'Modules', detail: 'engine, AI, samples, API in parallel' },
    { title: 'Integrate', detail: 'make the Python gate green' },
    { title: 'Review', detail: 'adversarial review then fix' },
  ],
}

const REPO = '/Users/franz/Desktop/DataPilot'
const COMMON = `
Repository: ${REPO}. Python venv: ${REPO}/.venv (use .venv/bin/python, .venv/bin/pytest, .venv/bin/ruff, .venv/bin/mypy). Set PYTHONPATH=services/api when running scripts. The package is installed editable, so imports work from tests.

FIRST read ${REPO}/docs/BUILD-SPEC.md completely. It is the single source of truth. Then read the current code you are replacing so you preserve invariants: services/api/datapilot/contracts/models.py, engine.py, governance.py, semantic.py, api/main.py, fixtures/clinical_nlp.py, serialization.py, and tests/.

Hard rules (from spec §11):
- Work ONLY in the files you own (listed in your task). If you must touch a shared file, make the smallest additive change and report it explicitly.
- Never run git stash/checkout/reset/commit or anything that changes git state. Never delete .data/. Never print or write secrets (an ANTHROPIC_API_KEY exists in the environment; never echo it).
- Strict typing (mypy --strict), ruff clean, pydantic strict models, from __future__ import annotations. No new dependencies beyond anthropic (already installed).
- Every human-facing string produced by the backend is bilingual: *_zh and *_en fields. No fixture column names hardcoded anywhere in engine/governance/ai.
- Other agents are editing other files in the same working tree concurrently. Do not reformat or rewrite files you do not own. Do not run repo-wide formatters. Running pytest on your own test files is fine; a failure in someone else's in-progress module is not yours to fix unless your task says so.
- Your final message is a report for the orchestrator: what you built, exact public names (classes/functions/signatures), what you verified (commands + results), known gaps, and any shared-file edits.
`

const MODELS_LIST = `
contracts/models.py must define (exact names; other agents import these):
Enums: RiskLevel, AuthorizationMode (POLICY_AUTHORIZED, HUMAN_APPROVAL_REQUIRED, QUARANTINE_ONLY, FORBIDDEN), ReleaseStatus (NOT_EVALUATED, BLOCKED, CONDITIONAL_PASS, PASS), AllowedAction (NORMALIZE_CATEGORY, STANDARDIZE_DATE_FORMAT, EXCLUDE_EXACT_DUPLICATE_FROM_RELEASE, EXCLUDE_COLUMN_FROM_RELEASE, QUARANTINE_RECORDS, FLAG_FOR_REVIEW), DecisionOutcome (APPROVE_PROPOSAL, QUARANTINE, EXCLUDE, FLAG_FOR_REVIEW, REJECT_PROPOSAL), Lifecycle (QUEUED, RUNNING, REVIEW_REQUIRED, OBSERVATIONAL, DRY_RUN_READY, APPLIED, FAILED), ContractSource (uploaded, drafted, sample, baseline), AITask (semantic, contract_draft, brief), AIStatus (ok, abstained, rejected_by_grounding, refusal, timeout, error, fallback_deterministic), ProviderName (anthropic, deterministic, verified-replay), EventStatus (STARTED, COMPLETED, FAILED, INFO).
Models: MetricScore(name, numerator, denominator, score, scope_zh, scope_en, applicable), ColumnProfile (spec §3.3 fields incl. TopValue(value, count, pattern_class|None) and FormatPattern(pattern, count)), ProfileSummary (existing fields), EvidenceSignal(signal, status, explanation_zh, explanation_en, evidence_ref), GroundingResult(valid, reason_codes, affected_record_count), AIProposalSummary(provider, model, prompt_version, input_hash, mapping: dict[str,str]|None, abstained, abstain_reason, grounding: GroundingResult, ledger_call_id), Finding (existing + title_zh, title_en, explanation_zh, explanation_en, allowed_outcomes: list[DecisionOutcome], sample_record_uids, proposal: AIProposalSummary|None), ContractInfo(id, version, hash, source: ContractSource, field_count), SensitivePreflight(columns_withheld, cells_masked), RunReport (existing + column_profiles, contract, sensitive_preflight, timings_ms: dict[str,int], warnings_zh, warnings_en, run_revision), HumanDecision(finding_id, outcome, reason|None, run_revision), actions NormalizeCategoryAction, StandardizeDateAction, ExcludeDuplicatesAction, QuarantineAction, ExcludeColumnAction, FlagAction(action_type Literal["FLAG_FOR_REVIEW"], finding_id, record_uids, authorization_source Literal["HUMAN"], authorization_ref), ApprovedAction discriminated union, DryRunReport (existing + flagged_record_count, blocking_unresolved: list[str]), ChangePreviewItem(record_uid, display_key, column, before, after, finding_id, action_type), ChangePreview(changes, totals: dict[str,int], truncated: bool), ValidationResult(check_id, passed, observed, expected, message_zh, message_en), ReleaseManifest (existing + flagged_record_uids, contract_hash, ai_call_count, ai_provider), ExecutionResult (existing), RunSummary(run_id, created_at, source_name, sample_id|None, record_count|None, column_count|None, lifecycle, release_status|None, contract_source|None, run_revision), RunEvent(seq, ts, stage, status: EventStatus, message_zh, message_en, elapsed_ms|None, detail: dict[str, Any]), ContractDraftField, ContractDraftCanonical(target, aliases), ContractDraftAmbiguity(column, tokens), ContractDraft(fields, business_key, ambiguity, notes_zh), RejectedRule(field, rule, reason_code, detail_zh, detail_en), ContractDraftResult(status: Literal["pending","ready","failed"], draft_yaml|None, accepted_rules: list[dict[str,Any]], rejected_rules: list[RejectedRule], ledger_call_id|None, error|None), ReleaseBriefClaim(text_zh, text_en, fact_ids, verified, reason|None), ReleaseBrief(status, summary_zh, summary_en, claims, verified_count, total_count, ledger_call_id|None), RedactionSummary(rows_sent, columns_withheld, values_sent, chars_sent), AICallRecord (spec §5.5), SemanticRequest (existing), AIProposal (existing), RunDetail(run_id, lifecycle, source_name, sample_id, created_at, run_revision, report|None, contract: ContractView|None, decisions: dict[str, HumanDecision], dry_run|None, preview|None, execution|None, brief|None, error|None), ContractView(yaml, parsed: dict[str,Any], hash, source), SampleInfo(id, title_zh, title_en, description_zh, description_en, rows, columns, has_contract, tags), RunCreated(run_id, lifecycle, run_revision), ApplyRequest (existing), DecisionsRequest(decisions: list[DecisionInput(finding_id, outcome, reason|None)]), ContractPutRequest(yaml), FromSampleRequest(sample_id, with_contract: bool), ErrorDetail(code, message_zh, message_en, retryable, correlation_id), ErrorResponse(error), DemoRelease (existing), HealthInfo(status, engine_version, ai: AIInfo(mode, provider, model, available), samples: int).
Keep StrictModel(strict=True, extra="forbid") as the base. Use Field(strict=False) on enum fields that must accept plain strings from JSON bodies (as HumanDecision.outcome does today).
`

phase('Foundation')
const foundation = await agent(`${COMMON}
YOUR TASK (B1, foundation). You own: services/api/datapilot/contracts/models.py, services/api/datapilot/contracts/policy.py, services/api/datapilot/storage.py, pyproject.toml (add anthropic>=1.3,<2 to dependencies only), tests/test_policy.py, tests/test_storage.py, fixtures/clinical_nlp/contract.yaml.
${MODELS_LIST}
Deliverables:
1. contracts/models.py as listed. Keep every existing model name that other code still imports (RunReport, Finding, DryRunReport, ExecutionResult, HumanDecision, ApplyRequest, SemanticRequest, AIProposal, GroundingResult, DemoRelease, RunCreated, ...) so the old engine.py/governance.py/api keep importing until they are replaced. New required fields on existing models are fine (the old modules are being rewritten concurrently) but prefer giving new fields sensible defaults where the spec allows, and NO defaults for fields the spec marks as always present in reports.
2. contracts/policy.py per spec §2: DataContract + FieldRule + ConsistentWith pydantic strict models, parse_contract(text) accepting v1 and v2 (translate v1 exactly as the spec says), ContractError(code, message_zh, message_en), contract_hash, baseline_contract, contract_to_yaml (stable key order, round-trips through parse_contract), plus helper accessors used by the engine: contract.required_fields(), contract.sensitive_fields(), contract.canonical_map(col) -> dict[alias, target], contract.allowed_values(col) -> set|None, contract.ambiguity_tokens(col) -> set, contract.date_fields() -> dict[col, FieldRule], contract.semantic_columns() -> list[str], contract.is_observational. Enforce limits.
3. fixtures/clinical_nlp/contract.yaml: the v2 equivalent of policy.yaml plus consistent_with on diagnosis_label (expected {Hypertension: [I10]}), ambiguity_registry.diagnosis_label [MS, RA, CVA, PCP], region canonical/allowed, free_text_note sensitive, record_id unique/required? (record_id is the business key; keep required_fields = diagnosis_code as in v1 so counts stay comparable), auto_authorization all true.
4. storage.py per spec §6 (RunStore). Events are JSONL with monotonically increasing seq per run; append_event returns the RunEvent. list_runs reads meta.json files; meta.json holds run_id, created_at (ISO UTC), source_name, sample_id, lifecycle, run_revision, record_count, column_count, release_status, contract_source, error. Provide update_meta(run_id, **fields). All writes via serialization.atomic_write_*; add atomic_append_line for JSONL (open in append mode + fsync is acceptable).
5. tests: v1→v2 translation golden (assert the translated contract equals parse of your new contract.yaml minus consistent_with/ambiguity extras, or assert specific fields), invalid YAML / non-mapping / limits raise ContractError with codes, contract_to_yaml round trip, contract_hash stable; RunStore create/read/events/ledger/list/delete on a tmp_path.
Verify: .venv/bin/pytest tests/test_policy.py tests/test_storage.py -q; .venv/bin/ruff check services/api/datapilot/contracts services/api/datapilot/storage.py tests/test_policy.py tests/test_storage.py; .venv/bin/mypy services/api/datapilot/contracts services/api/datapilot/storage.py. Do NOT try to make the whole old test suite pass; other modules are being rewritten.`,
  { label: 'B1:foundation', phase: 'Foundation', effort: 'high' })
log('foundation done')

phase('Modules')
const modules = await parallel([
  () => agent(`${COMMON}
FOUNDATION REPORT (models/policy/storage already exist; import from them):
${foundation}

YOUR TASK (B2, engine + governance). You own: services/api/datapilot/engine/ (new package: __init__.py, parse.py, profile.py, detectors.py, sensitive.py), deleting the old services/api/datapilot/engine.py once the package exists, services/api/datapilot/governance.py, tests/test_engine.py, tests/test_governance.py, tests/test_sensitive.py.
Implement spec §3 and §4 exactly. Public API that other modules rely on:
- datapilot.engine: analyze_csv(content: bytes, contract: DataContract | None = None, *, synthetic=False, fixture_version=None, ai: "SemanticResolver | None" = None, run_revision: int = 1) -> RunReport; baseline_policy() (returns baseline_contract() for compatibility), load_policy(path) -> DataContract (parses v1 or v2), AnalysisError(code, message_zh, message_en), ENGINE_VERSION = "0.2.0", parse_csv(content) -> (pl.DataFrame of all-string columns, delimiter), record_uids(dataset_hash, n), build_profile(...), mask_value(value, pattern_class) helpers as you see fit.
- The semantic step must be pluggable: define a Protocol SemanticResolver with resolve(request: SemanticRequest, *, run_id: str | None) -> tuple[AIProposal, GroundingResult, str | None ledger_call_id]. The engine builds the SemanticRequest (candidate value counts, canonical vocabulary, evidence refs, ambiguity tokens) per SEM column, calls the resolver if provided, otherwise uses the deterministic normalize()-match fallback described in spec §3.5, and records the outcome on Finding.proposal. Never call Anthropic from the engine; the AI package (another agent) implements the resolver and the pipeline wires it in. Also expose a function semantic_candidates(frame, contract) so the pipeline can emit an event with the number of SEM columns before calling AI.
- Detectors are generic and contract-driven; ids per spec table; each finding has title_zh/en, explanation_zh/en (templated with column name and counts), allowed_outcomes, sample_record_uids (first 20), evidence signals with zh/en explanations.
- Metrics per §3.4 with scope strings, e.g. scope_zh="必填字段 1 个 × 5,200 行".
- Column profiles per §3.3 (types inferred from strings; date patterns detected via regex families YYYY-MM-DD, DD/MM/YYYY, YYYY/MM/DD, YYYYMMDD, ISO datetime).
- Sensitive masking per §3.5; raw matches must never be in any output (write a test that dumps the report JSON and asserts the planted values are absent).
- governance.py: prepare_dry_run(report, decisions, contract, *, run_revision) per §4; preview_changes(source, report, dry_run, limit=50); execute(source, contract, report, dry_run) with the generic validation list in §4 (every validation bilingual); ExecutionBundle unchanged in shape; demo_decisions(report) now derives decisions generically: APPROVE_PROPOSAL for SEM findings, QUARANTINE for quarantine-only, EXCLUDE for PHI.
- Performance: 250k rows × 200 cols must not blow up; avoid per-cell Python loops where Polars expressions work, but correctness first. The 5,200-row clinical sample must analyze in under 2 s and the 8,000-row ecommerce sample in under 3 s on a laptop.
Tests: use fixtures/clinical_nlp/contract.yaml + datapilot.fixtures.clinical_nlp.generate_csv_bytes() (still exists) and assert: findings ids present {DUP-EXACT, CAT-region, FMT-encounter_date, SEM-diagnosis_label, SEM-diagnosis_label-CONFLICT, AMB-diagnosis_label, MISS-diagnosis_code, PHI-free_text_note} with counts 43/61/72/183 supported+1 conflict/8/27/3; dry run + execute end-to-end with demo_decisions gives CONDITIONAL_PASS, eligible 5,121, quarantined 36, excluded 43, all validations pass, deterministic release hash; a synthetic generic CSV (make one inline: 12 arbitrary columns, mixed types, dupes, a phone in a "备注" column) works with and without a contract and never hits StopIteration; REJECT_PROPOSAL on SEM keeps BLOCKED; FLAG_FOR_REVIEW keeps rows in release and lists them in manifest.flagged_record_uids.
Verify with pytest on your files, ruff, mypy on the engine package + governance.py.`,
    { label: 'B2:engine+governance', phase: 'Modules', effort: 'high' }),

  () => agent(`${COMMON}
FOUNDATION REPORT (models/policy/storage already exist; import from them):
${foundation}

YOUR TASK (B3, AI layer). You own: services/api/datapilot/ai/ (new package: __init__.py, provider.py, redaction.py, grounding.py, ledger.py, prompts.py, tasks/__init__.py, tasks/semantic.py, tasks/contract_draft.py, tasks/brief.py), services/api/datapilot/semantic.py (turn into a thin shim re-exporting AnthropicProvider, VerifiedReplayProvider/DeterministicProvider, validate_proposal from ai/), scripts/llm_smoke.py, tests/test_semantic.py, tests/test_ai_grounding.py, tests/test_ai_redaction.py, .env.example (remove ANTHROPIC_QUALITY_MODEL, add DATAPILOT_AI_MODE=auto, ANTHROPIC_MODEL=claude-opus-5), .github/workflows/anthropic-smoke.yml (remove the quality input; keep a manual smoke).
Implement spec §5 exactly. Anthropic SDK facts verified against the installed anthropic==1.3.0: client.beta.messages.create accepts model, max_tokens, system, messages, output_config, betas, fallbacks, timeout; client.messages.create has output_config and NO temperature; Opus 5 rejects temperature/top_p and budget_tokens; omit thinking (adaptive default); structured output shape is output_config={"format": {"type": "json_schema", "schema": {...}}, "effort": "low|medium|high"}; check response.stop_reason == "refusal" before reading content; parse the first content block with type == "text" via json.loads. Reference docs for the SDK are at /private/tmp/claude-501/bundled-skills/2.1.255/3a634b68e9d59eb51d90a93c633ffb07/claude-api/python/claude-api/README.md and tool-use.md (read the Structured Outputs section). Use anthropic.Anthropic() (credentials resolve from the environment; never pass or print the key).
Public API (the pipeline agent imports these):
- datapilot.ai.get_runtime(store: RunStore | None = None) -> AIRuntime, where AIRuntime has: info() -> AIInfo(mode, provider, model, available); semantic_resolver(run_id) -> SemanticResolver (Protocol defined in the spec for the engine: resolve(request, *, run_id) -> (AIProposal, GroundingResult, ledger_call_id|None); implement it here so the engine can be called with ai=runtime.semantic_resolver(run_id)); draft_contract(run_id, report: RunReport) -> ContractDraftResult; brief(run_id, report, execution: ExecutionResult | None) -> ReleaseBrief. Every call appends an AICallRecord via store.append_ledger (or an in-memory list when store is None) and enforces the ≤ 8 calls/run bound (read existing ledger length first).
- provider.py: LLMProvider Protocol with complete_json(task: AITask, system: str, user_payload: dict, schema: dict, *, effort: str, max_tokens: int, timeout_s: float) -> ProviderResult(data: dict | None, status: AIStatus, model_served, input_tokens, output_tokens, cache_read_tokens, latency_ms, request_id, error). AnthropicProvider (SDK as above, prompt caching on the system block), DeterministicProvider (name "deterministic" or "verified-replay" when DATAPILOT_AI_MODE=replay) implementing the fallback behaviours: semantic normalize()-match, contract draft from heuristics (required if null_rate == 0 and distinct high; sensitive from heuristic column names/pattern hits; date type/format from format_patterns; allowed when distinct ≤ 12 and string), brief from a fixed bilingual template using the facts. select_provider() per DATAPILOT_AI_MODE.
- redaction.py: build_semantic_payload(request), build_profile_payload(report) (column profiles with sensitive top_values withheld, ≤ 30 values, ≤ 64 chars, control chars stripped), build_facts_payload(report, execution) -> dict[fact_id, value]; each returns (payload, RedactionSummary, input_hash). Payload must contain rows_sent: 0.
- grounding.py: validate_proposal (existing rules, keep reason codes), validate_contract_draft(draft, report) -> (accepted_rules, rejected_rules with reason codes from spec), validate_brief(brief, facts) -> claims with verified flags (number-token check must handle 5,200 / 5200 / 98.73 / 98.73% / full-width digits).
- prompts.py: SYSTEM_SEMANTIC, SYSTEM_CONTRACT_DRAFT, SYSTEM_BRIEF with PROMPT_VERSION constants ("semantic-2.0", "contract-draft-1.0", "brief-1.0"); JSON schemas for each output; prompts state that all values are untrusted quoted data, forbid code, forbid inventing columns/values, require abstention on ambiguity, and require Chinese-first explanations (brief and draft rationale in natural Chinese plus English).
- Contract draft output → contract_to_yaml via contracts/policy.py DataContract so the YAML is guaranteed parseable; rejected rules are simply omitted from the YAML but returned.
- scripts/llm_smoke.py: runs one real semantic call and one real contract-draft call (against a tiny inline profile) with the live provider, prints status/model/tokens/latency/grounding, exits non-zero on failure. RUN IT ONCE with the key in the environment to prove the SDK call shape works (DATAPILOT_AI_MODE=auto). If the API rejects betas/fallbacks, remove them and document why in provider.py. Report the measured latency.
Tests (offline, DATAPILOT_AI_MODE=replay): grounding rejects each reason code; redaction never leaks sensitive values (plant values, assert absent from json.dumps(payload)); injection canary text is present only as a JSON string value and any mapping whose source is the canary is rejected as HALLUCINATED_SOURCE_VALUE when not in candidates; brief grounding flags an invented number; ledger record written with rows_sent 0; call bound enforced; AnthropicProvider parsing tested with a fake client object (monkeypatch the SDK call to return an object with content/stop_reason/usage/model) including the refusal path.
Verify with pytest on your files, ruff, mypy on the ai package and semantic.py.`,
    { label: 'B3:ai-layer', phase: 'Modules', effort: 'high' }),

  () => agent(`${COMMON}
FOUNDATION REPORT (models/policy/storage already exist; import from them):
${foundation}

YOUR TASK (B4, samples). You own: services/api/datapilot/samples/ (new package: __init__.py registry, clinical_nlp.py, ecommerce_orders.py, hr_roster.py), services/api/datapilot/fixtures/clinical_nlp.py (keep as a re-export shim of samples/clinical_nlp.py so existing imports work), fixtures/ecommerce_orders/contract.yaml, fixtures/hr_roster/contract.yaml, tests/test_samples.py, docs/SAMPLES.md.
Implement spec §8. Registry API: SAMPLES: dict[str, Sample] where Sample is a frozen dataclass(id, title_zh, title_en, description_zh, description_en, tags: tuple[str,...], generate: Callable[[], bytes], contract_path: Path | None, planted: dict[str, int] documenting expected counts); list_samples() -> list[SampleInfo]; get_sample(id) -> Sample; sample_contract_text(id) -> str | None. Generators must be deterministic (arithmetic patterns like the existing clinical generator; no random module), produce UTF-8 CSV with a header, realistic-looking Chinese business data, and plant EXACTLY the issues in the spec table with the stated counts; record the counts in Sample.planted and in docs/SAMPLES.md (tables per sample: column, meaning, planted issue, count, expected finding id). Contracts are v2 YAML per spec §2 and must parse with datapilot.contracts.policy.parse_contract (write that assertion in tests). The ecommerce remark column must include the exact injection canary sentence from the spec in ONE cell and phone numbers in 18 cells. HR id_card values must match the CN ID pattern (17 digits + digit/X) but be obviously synthetic (e.g. start with 000000). Keep the clinical generator byte-identical to today (the golden artifacts depend on it): move the code, do not change output; add a test asserting sha256 of generate_csv_bytes() equals the current value (compute it first from the existing module and hard-code it).
Also write tests that, once the engine package exists (another agent is building services/api/datapilot/engine/ concurrently), assert the expected finding ids and counts for ecommerce_orders and hr_roster with their contracts; mark those tests with pytest.importorskip("datapilot.engine.detectors") so they skip cleanly until the engine lands, and document in your report the exact expected counts so the integration agent can reconcile.
Verify: pytest tests/test_samples.py, ruff, mypy on the samples package.`,
    { label: 'B4:samples', phase: 'Modules', effort: 'high' }),

  () => agent(`${COMMON}
FOUNDATION REPORT (models/policy/storage already exist; import from them):
${foundation}

YOUR TASK (B5, pipeline + API). You own: services/api/datapilot/pipeline.py, services/api/datapilot/api/main.py (rewrite), services/api/datapilot/api/sse.py, services/api/datapilot/api/errors.py, tests/test_api.py (rewrite), tests/test_pipeline.py, tests/test_golden_artifacts.py (adjust paths only if needed), scripts/generate_golden.py (update to the new engine/governance/sample APIs and DATAPILOT_AI_MODE=replay), Makefile (add targets: api, web, demo (starts both), golden), docker-compose.yml + Dockerfile.api (env vars for AI mode/model pass-through; no secrets baked), README.md (update run instructions and env table; keep truth boundaries section).
Implement spec §6 (pipeline) and §7 (API) exactly. The engine package, AI package, and samples package are being written concurrently by other agents against these public names: datapilot.engine.analyze_csv(content, contract, *, ai=..., run_revision=...), datapilot.engine.semantic_candidates(frame, contract), datapilot.engine.parse_csv, datapilot.engine.AnalysisError, ENGINE_VERSION; datapilot.governance.prepare_dry_run(report, decisions, contract, *, run_revision), preview_changes(source, report, dry_run, limit), execute(source, contract, report, dry_run), GovernanceError, demo_decisions(report); datapilot.ai.get_runtime(store) -> AIRuntime with info(), semantic_resolver(run_id), draft_contract(run_id, report), brief(run_id, report, execution); datapilot.samples.list_samples(), get_sample(id) (Sample has .generate(), .contract_path, .title_*), sample_contract_text(id); datapilot.contracts.policy.parse_contract, baseline_contract, contract_hash, contract_to_yaml. Code against these names. If at the end some import does not exist yet, leave your code correct per the spec and list the mismatch in your report; the integration agent reconciles.
Details:
- Async execution: ThreadPoolExecutor(max_workers=2) module-level; DATAPILOT_SYNC_PIPELINE=1 runs inline. Each stage wraps in try/except → FAILED event + meta.lifecycle FAILED + error stored; never leave a run in RUNNING forever. Semantic stage emits detail {sem_columns, provider, model} and, on fallback, {fallback_reason}.
- SSE: async generator; on connect, replay events with seq > after from disk, then poll the file every 250 ms for new lines; send ": heartbeat" comment every 10 s; stop after a terminal lifecycle event when the run is no longer RUNNING/QUEUED and no draft/brief job is active, or when the client disconnects. Content-Type text/event-stream, headers Cache-Control: no-cache, X-Accel-Buffering: no.
- Draft and brief endpoints run their own pipeline job (events CONTRACT_DRAFTING / BRIEF_DRAFTING). PUT /contract parses (422 with ContractError codes on failure), bumps run_revision, clears decisions/dry-run/execution files, re-runs analysis (async).
- Decisions PUT validates finding ids and allowed_outcomes (422 with the offending id), persists decisions.json, invalidates dry-run.json, returns unresolved list. dry-run 409 when unresolved or NOT_EVALUATED. apply: idempotency by (run_id, key) persisted in meta; 409 on stale revision/hash or BLOCKED validations.
- Errors: register exception handlers so every error body matches spec §7 (code, message_zh, message_en, retryable, correlation_id = uuid4 hex; also set X-Correlation-Id header). Map AnalysisError/ContractError/GovernanceError → 422/409 with their codes; HTTPException → generic codes; unexpected → 500 INTERNAL with correlation id and a server-side log line.
- Artifacts: FileResponse for csv/json; audit-bundle.json assembled on the fly.
- Health: reads AIRuntime.info(), sample count, engine version, data dir writability.
- Keep GET /v1/demo/clinical-nlp and /release working (use the clinical sample + contract + replay AI mode).
- Optional bearer: if DATAPILOT_API_TOKEN is set, require Authorization: Bearer on /v1/* except GET /health.
Tests (DATAPILOT_SYNC_PIPELINE=1, DATAPILOT_AI_MODE=replay, DATAPILOT_DATA_DIR=tmp_path via monkeypatch before importing the app or via a factory create_app(data_dir)): prefer a create_app(settings) factory in api/main.py with module-level app = create_app() for uvicorn. Test the full flow for the clinical sample via /v1/runs/from-sample → GET run → decisions → dry-run → apply → artifacts; observational upload; PUT contract flow on an uploaded CSV; SSE endpoint returns the persisted events (use TestClient stream with a small after param and assert the first event); 409/422 paths; restart safety (second create_app on the same data dir sees the run). Tests that depend on other agents' modules will fail until they land; write them anyway, run what you can, and report precisely.
Verify: ruff + mypy on your files; pytest for what can run.`,
    { label: 'B5:pipeline+api', phase: 'Modules', effort: 'high' }),
])
const reports = modules.filter(Boolean)
log(`${reports.length}/4 module agents reported`)

phase('Integrate')
const integration = await agent(`${COMMON}
YOUR TASK (B6, integration). You own everything under services/api/, tests/, scripts/, fixtures/, docs/DEMO.md, docs/SAMPLES.md, Makefile, pyproject.toml for this phase (all backend agents have finished; nobody else edits backend files now; frontend agents may be editing app/, lib/, components/ concurrently — do not touch those).
Module reports from the four builders (public names, gaps, mismatches):
${JSON.stringify(reports, null, 1)}

Goal: make the Python quality gate green and the system coherent end-to-end:
1. .venv/bin/pytest -q must pass entirely (all tests, including the module tests and any importorskip'd sample tests, which must now run — remove the importorskip guards).
2. .venv/bin/ruff check services tests conftest.py scripts must pass; .venv/bin/mypy services/api/datapilot must pass strictly.
3. Reconcile interface mismatches between engine/ai/samples/api per the spec (the spec wins; if the spec is ambiguous, pick the simplest and update docs/BUILD-SPEC.md in place).
4. Reconcile planted-issue counts for ecommerce_orders and hr_roster: run the engine with each contract, compare to Sample.planted / docs/SAMPLES.md, and fix whichever side is wrong (generator, contract, or detector) so the documented counts are the truth. Every sample with its contract must go dry-run → apply → CONDITIONAL_PASS with demo_decisions and all validations passing; and each sample without contract must analyze observationally.
5. Regenerate golden artifacts: PYTHONPATH=services/api DATAPILOT_AI_MODE=replay .venv/bin/python scripts/generate_golden.py; then update docs/DEMO.md numbers from the regenerated report (findings ids changed to the generic scheme; keep the document truthful) and make tests/test_golden_artifacts.py pass.
6. Start the API for real: DATAPILOT_DATA_DIR=.data-dev .venv/bin/uvicorn datapilot.api.main:app --port 8010 (port 8000 is occupied by an old docker container; use 8010) in the background, then exercise with curl: /health, /v1/samples, POST /v1/runs/from-sample for ecommerce_orders with contract, stream /v1/runs/{id}/events with curl -N for a few seconds, GET run, PUT decisions (derive from allowed_outcomes), dry-run, apply, artifacts, ai-ledger, brief. With the real ANTHROPIC key present (DATAPILOT_AI_MODE=auto) confirm the SEM finding shows provider anthropic and the ledger has real token counts; report the measured latencies. Also run the contract-draft flow on an uploaded observational CSV (use a sample without contract) and report accepted/rejected rule counts. Kill the uvicorn process when done and remove .data-dev.
7. Record the final counts (tests passed, findings per sample, latencies) in your report.
Do not weaken tests to make them pass; fix the code. Do not change git state.`,
  { label: 'B6:integration', phase: 'Integrate', effort: 'high' })
log('integration done')

phase('Review')
const FINDINGS = {
  type: 'object', additionalProperties: false, required: ['findings'],
  properties: { findings: { type: 'array', items: { type: 'object', additionalProperties: false, required: ['title', 'file', 'severity', 'evidence', 'fix'],
    properties: { title: { type: 'string' }, file: { type: 'string' }, severity: { type: 'string', enum: ['blocker', 'major', 'minor'] }, evidence: { type: 'string', description: 'What you ran or read that proves it' }, fix: { type: 'string' } } } } },
}
const reviewCommon = `${COMMON}
INTEGRATION REPORT:
${integration}

You are a REVIEWER. Read-only for source files: do not edit code. You may write scratch scripts under /private/tmp/claude-501/-Users-franz-Desktop-DataPilot/0cb7279f-5136-43eb-8548-82720a923972/scratchpad and run them (PYTHONPATH=services/api, DATAPILOT_AI_MODE=replay unless your lens needs live AI). Report only findings you actually verified with evidence (a command, an output, a line). Severity: blocker = would fail or embarrass in the live job-fair demo or violates a truth boundary; major = wrong behaviour on plausible input; minor = quality.`
const reviews = (await parallel([
  () => agent(`${reviewCommon}
LENS: invariants and correctness. Verify with scripts: source immutability, record_uid stability across revisions, score scope invariance (quarantine/exclusion never shrink denominators), idempotent apply, deterministic release hash, NO_UNAPPROVED_CELL_CHANGES really diffs cells, FLAG_FOR_REVIEW rows retained, REJECT_PROPOSAL keeps BLOCKED, run_revision bump clears decisions, restart safety of the store, PASS vs CONDITIONAL_PASS logic, metrics applicability without contract, and that no fixture column name remains hardcoded anywhere in engine/, governance.py, ai/ (grep for encounter_date, diagnosis_label, diagnosis_code, region, I10, E11.9, SEM-004).`,
    { label: 'review:invariants', phase: 'Review', schema: FINDINGS, effort: 'high' }),
  () => agent(`${reviewCommon}
LENS: security, redaction, prompt injection, AI supervision honesty. Verify: sensitive values never appear in report JSON, events, ledger, preview, brief, draft, or AI payloads (plant emails/phones/CN IDs in a CSV and grep every artifact and the AI payload builders); the injection canary is a JSON string value only; grounding rejects unknown columns/values/evidence/targets for all three tasks; refusal/timeout/error paths produce fallback_deterministic status and the report never labels deterministic output as anthropic; ledger call bound; API error bodies never include stack traces or raw values; CSV formula-injection risk on exported release.csv (cells starting with = + - @) — decide whether the release CSV should be byte-faithful (it must, since source immutability is a promise) and whether the UI/downloads need a warning; upload limits enforced before parsing; DATAPILOT_API_TOKEN check when set.`,
    { label: 'review:security', phase: 'Review', schema: FINDINGS, effort: 'high' }),
  () => agent(`${reviewCommon}
LENS: robustness on hostile and odd inputs, live-demo failure modes. Feed the real API (start uvicorn on port 8011 with a tmp data dir, kill it after) with: BOM CSV, semicolon and tab delimiters, quoted fields with embedded newlines and commas, a single-column CSV, a header-only CSV, 200 columns, a 250,001-row CSV (build it fast), non-UTF-8 bytes, a column named with spaces/Chinese/emoji, duplicate rows only, all-null column, a date column with 3 formats, a contract referencing unknown columns, a contract with 0 fields, a huge allowed list, decisions with wrong outcomes, dry-run before decisions, apply twice with same/different idempotency keys, delete then fetch, SSE reconnect with after=seq, concurrent two uploads. Also measure analysis time for the 8,000-row ecommerce sample and for a 100k-row synthetic CSV and report them. Report anything that 500s, hangs, is slow (> 5 s for 100k rows) or leaves a run in RUNNING.`,
    { label: 'review:robustness', phase: 'Review', schema: FINDINGS, effort: 'high' }),
])).filter(Boolean)
const allFindings = reviews.flatMap((r) => r.findings)
log(`${allFindings.length} review findings (${allFindings.filter((f) => f.severity === 'blocker').length} blockers)`)

const fixReport = await agent(`${COMMON}
YOUR TASK (B7, fix). You own all backend files (services/api/, tests/, scripts/, fixtures/, docs/BUILD-SPEC.md, docs/DEMO.md, docs/SAMPLES.md). Three reviewers produced these verified findings:
${JSON.stringify(allFindings, null, 1)}

Fix every blocker and major. Fix minors when cheap. For each finding, add or extend a test that would have caught it. If you disagree with a finding, verify first; if it is not real, say why in your report instead of changing code. After fixing, run the full Python gate: .venv/bin/pytest -q && .venv/bin/ruff check services tests conftest.py scripts && .venv/bin/mypy services/api/datapilot. Regenerate golden artifacts if engine output changed (DATAPILOT_AI_MODE=replay) and keep docs/DEMO.md truthful. Report: per finding → fixed/not-real/deferred with reason; final gate output summary.`,
  { label: 'B7:fix', phase: 'Review', effort: 'high' })

return { foundation, reports, integration, findings: allFindings, fixReport }