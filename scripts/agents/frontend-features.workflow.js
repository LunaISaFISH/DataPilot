export const meta = {
  name: 'datapilot-frontend-features',
  description: 'Build the DataPilot v0.2 console UI on top of the frontend foundation: home, history, engine page, run console (three panes + API log drawer), replay restyle, then typecheck/lint/build',
  phases: [
    { title: 'Skeleton', detail: 'console shell + context + home/history/engine' },
    { title: 'Panes', detail: 'findings/AI envelope/decisions, change set/validation/artifacts, replay' },
    { title: 'Gate', detail: 'tsc, lint, build, dead-code cleanup' },
  ],
}

const REPO = '/Users/franz/Desktop/DataPilot'
const f1 = args && args.f1Report ? args.f1Report : '(no foundation report supplied — read lib/ and components/datapilot/ directly)'

const COMMON = `
Repository: ${REPO} (vinext 1.0.0-beta.9 = Next.js App Router compatible on Vite 8, React 19, Tailwind 4, shadcn/base-ui in components/ui, TypeScript strict, oxlint). Node 22. Run npm/npx from the repo root.

FIRST read ${REPO}/docs/BUILD-SPEC.md completely — especially §5.5–5.6 (ledger, permission card, red-team), §7 (HTTP API), §9 (frontend: console layout, panes, cleanup), §12 (demo ops). Then read the foundation that already exists: lib/types.ts, lib/api.ts, lib/labels.ts, lib/format.ts, lib/language.tsx, components/datapilot/index.ts and every component it exports, app/layout.tsx, app/globals.css, app/kit/page.tsx. Foundation agent report (names/signatures/deviations):
${f1}

Hard rules: no new npm dependencies; TypeScript strict, no any; every user-facing string bilingual via t(en, zh) with Chinese as the primary reading; enum/stage labels via lib/labels.ts; finding titles/explanations come from the API (*_zh/*_en), never hardcoded per finding id; no fake data, no simulated progress, no animations not backed by real elapsed time, no emoji, no exclamation marks, no marketing copy; hashes via HashChip; every AI-derived element carries the ProvenanceMark; 409/422 errors render as designed guard rows using ApiError.code + message_zh, never toasts only. Never run git state-changing commands. Do not touch services/, tests/, scripts/*.py (backend agents are working there). Do not start servers on ports 3000/8000 (an old docker stack holds them); if you need a dev server use another port (e.g. npm run dev -- --port 3010; check vinext CLI flags) and stop it afterwards. The backend v0.2 API is being built concurrently and is NOT available yet — build strictly against lib/types.ts + the spec; verify with npx tsc --noEmit -p tsconfig.json and npm run lint and npm run build. Work only in the files you own; if you must touch a shared file make the smallest additive change and report it. Your final message is a report for the orchestrator: files created/changed, exported names, deviations from the spec, verification commands + results, known gaps.
`

phase('Skeleton')
const skeleton = await parallel([
  () => agent(`${COMMON}
YOUR TASK (F3a, run console skeleton + client extensions). You own: lib/api.ts (extend), lib/types.ts (extend), lib/api-log.ts (new), lib/hash.ts (new), app/runs/[id]/page.tsx, app/runs/[id]/run-workspace.tsx, app/runs/[id]/workspace-context.tsx, app/runs/[id]/sections/header-strip.tsx, sections/lifecycle-rail.tsx, sections/event-log-panel.tsx, sections/tabs.tsx (tab container only), sections/profile-tab.tsx, sections/contract-tab.tsx, sections/artifacts-tab.tsx, sections/ai-supervision-rail.tsx, sections/api-log-drawer.tsx, app/runs/[id]/sections/README.md (the contract between panes: context shape, which tab/pane files other agents own and the props they receive).
1. Extend lib/types.ts with everything §5.5–5.6/§7/§4 added after the foundation: AICallRecord fields (finding_id, request_bytes, request_payload, response_payload, grounding.affected_record_count, status 'cached'), VerifyReport{ok, checks: ValidationResult-like[]}, ArtifactInfo{name, role, bytes, sha256, modified_at}, RedteamCase union ('HALLUCINATED_SOURCE_VALUE'|'UNKNOWN_CANONICAL_TARGET'|'UNKNOWN_EVIDENCE_REFERENCE'|'UNSUPPORTED_ACTION'|'STALE_OR_UNKNOWN_INPUT'|'ABSTENTION_WITH_MAPPING'|'AMBIGUITY_REGISTRY_HIT'|'LIVE_INJECTION'|'TIMEOUT'), RedteamResult{case, original_proposal, tampered_proposal, grounding, ledger_call_id}, AiContract (per §5.6), TamperTestResult (ExecutionResult & {written:false}), DryRunReport.decision_set_hash, ReleaseManifest.decision_set_hash/change_ledger_hash/ai_input_hashes, ApiError body with observed/expected, ProfileSummary.source_encoding, RunDetail.parent_run_id?. Keep existing names.
2. Extend lib/api.ts: getAiContract, redteam(runId, findingId, case), rerunSemantic(runId, findingId), listArtifacts, verifyRun, replayRun, tamperTest, cleanupRuns(olderThanMinutes), plus header Idempotency-Key on applyRun and parsing of X-Idempotent-Replay / X-Correlation-Id / Server-Timing on every response. Every request/response (including SSE open/close/error events) is recorded into lib/api-log.ts: a tiny external store (subscribe/getSnapshot for useSyncExternalStore) holding up to 200 entries {id, time, method, path, status, serverMs, clientMs, correlationId, idempotentReplay, requestBody (truncated 64 KB, never file bytes), responseBody (truncated), error}. Export useApiLog().
3. lib/hash.ts: sha256Hex(ArrayBuffer) via crypto.subtle; nothing else.
4. app/runs/[id]/page.tsx (client) → RunWorkspace. run-workspace.tsx: loads RunDetail, subscribes to events via subscribeEvents (with the foundation's polling fallback), refreshes RunDetail on terminal/stage events and after every mutation, exposes everything through workspace-context.tsx: {run, events, health, language, selectedFindingId, setSelectedFindingId, activeTab, setActiveTab, refresh(), busy flags, mutate helpers: putContract, draftContract, putDecisions, createDryRun, applyRun, rerunSemantic, redteam, tamperTest, verifyRun, replayRun}. Handle lifecycles: QUEUED/RUNNING (show live log prominently, tabs that need a report are locked with the reason), FAILED (error guard row with correlation id), OBSERVATIONAL, REVIEW_REQUIRED, DRY_RUN_READY, APPLIED.
5. Layout per §9.2: header strip (source name, run id HashChip, revision, source_encoding badge, contract pill, dataset HashChip, 质量分 mono large next to ReleaseStatusPill, counts 记录/字段/问题, AI counters 提议/弃权/被拒 derived from findings[].proposal + ledger), left rail 240px (LifecycleRail derived from run state with the artifact file name and timestamp for each state, then EventLogPanel = the real event stream), center tabs (画像 · 契约 · 发现 · 处置 · 变更集 · 验证与发布 · 工件; default 发现 when report exists else 画像; tabs owned by other agents are mounted from sections/findings-tab.tsx, decisions-tab.tsx, changeset-tab.tsx, release-tab.tsx which you create as minimal typed stubs that render an EmptyState '由其他模块提供' so the build passes — those files will be replaced), right pane 400px (when a finding is selected → mount sections/finding-inspector.tsx (stub now); otherwise → your AiSupervisionRail: permission card from getAiContract (可见 / 不可见 / 可提议 / 不可 lists, schema viewer as collapsible mono JSON, prompt versions, limits), ledger DataTable (task, model, status, tokens in/out, latency, grounding), budget meter as plain numbers (calls used / 8), provider/mode), bottom drawer ApiLogDrawer (collapsed 28px bar with last request summary; expanded table time/METHOD path/status/serverMs/correlationId; failed rows red with the structured error body; click expands request/response JSON in mono).
6. Tabs you implement fully: 画像 (metric tiles with scope text and 不适用 + reason; column profile DataTable: name, type, 非空率 Bar, 唯一值, top 值 (MaskedValue when pattern_class), 格式分布, 标记 pills, min/max; timings_ms list; sensitive_preflight summary), 契约 (no contract → 仅观测模式 explanation, button 让 AI 起草契约 → draftContract → CONTRACT_DRAFTING events visible in the log → poll getContractDraft until ready/failed → accepted rules DataTable (field, rule, evidence chips) + rejected rules DataTable (field, rule, reason code pill, detail_zh) + YamlEditor prefilled with draft_yaml + button 确认契约并重新分析 → putContract; contract present → rule summary table derived from contract.parsed + YamlEditor readOnly + HashChip + 替换契约 (YamlEditor editable + submit)), 工件 (listArtifacts DataTable: name, role, bytes, sha256 HashChip, modified; 重新校验 → verifyRun → checks table; copyable commands shasum -a 256 <file> and python -m datapilot verify <run_dir> using CopyButton; 重跑同一文件 → replayRun → link to the new run and an equality strip comparing dataset/scope/evaluation-scope/action-set/release hashes of parent and child when both are available (fetch child run detail)).
7. Write sections/README.md documenting the context API and the exact props/contract for the stubbed files so F3b/F3c/F5 can implement them without touching your files.
Verify: tsc, lint, build.`,
    { label: 'F3a:console-skeleton', phase: 'Skeleton', effort: 'high' }),

  () => agent(`${COMMON}
YOUR TASK (F2, home + history + engine page + cleanup). You own: app/page.tsx (rewrite), app/runs/page.tsx (new), app/runs/new/page.tsx (redirect to / via next/navigation redirect or a client replace), app/engine/page.tsx (new), app/service-worker-registration.tsx (delete) and its import in app/layout.tsx (remove — minimal edit to a shared file; report it), public/sw.js (delete), app/chatgpt-auth.ts (delete), db/ (delete), drizzle.config.ts (delete), package.json scripts db:generate (remove), env.d.ts and db/env.d.ts (remove db-related types if they reference cloudflare bindings no longer used; keep the build green), app/runs/new/upload-workspace.tsx (delete), README.md frontend section is owned by backend agents — do not edit.
1. app/page.tsx 工作台 per §9.2: page title 数据发布工作台 with one 13px positioning sentence (AI 提议 · 策略决策 · 人工拍板 · 规则执行 · 哈希对账 / AI proposes · policy decides · humans approve · rules execute · hashes reconcile); left: 新建发布评估 card with a real drag-and-drop zone (also click to pick) for CSV + optional contract YAML, showing file name, size, and an encoding hint (read the first 4 KB with FileReader and note UTF-8 BOM / likely GB18030 when decoding as UTF-8 fails — display only), button 开始分析 → createRun → router.push('/runs/{id}'); right: samples from listSamples rendered as compact cards (title_zh, rows × columns, description, tags, buttons 带契约分析 / 仅观测 → createRunFromSample → push); below: 最近运行 DataTable (run id chip, source, sample, records, columns, contract source, LifecyclePill, ReleaseStatusPill, created) limited to 8 with link to /runs, header actions 刷新 and 重置演示 (ConfirmDialog → cleanupRuns(0) → refresh). When resolveApiBase() is null (published replay-only site) show an InlineAlert explaining live analysis needs the local API and link to /demo/clinical-nlp. No hero, no badges, no dark promo card.
2. app/runs/page.tsx: full history DataTable with actions 打开 / 重跑同一文件 (replayRun then push) / 删除 (ConfirmDialog → deleteRun), filters by lifecycle, refresh.
3. app/engine/page.tsx 关于引擎: sections — 不变量 (from README truth boundaries + spec §0, as a definition list), AI 边界 (an inline SVG diagram, theme-safe, showing 数据 → 脱敏聚合 → 模型 → 接地校验 → 策略 → 人工 → 确定性执行器 → 验证 → 复核, with the never-visible list), 允许的动作 table (the six AllowedAction values with zh meaning and who may authorize), 验证门禁 table (the validation check ids from lib/labels.ts with zh meaning), 接地原因码 table, 数据契约 v2 short example (YAML in mono), ADR summaries (read docs/adr/*.md and summarise faithfully), 质量门 (the make test command list; do not invent counts — say what the gate runs). Live values (engine version, model, provider) come from useHealth.
4. Cleanup per spec §9.3: perform the deletions listed above, grep to make sure nothing imports them, keep the build green. Do NOT delete components/ui files (F6 does that at the end).
Verify: tsc, lint, build.`,
    { label: 'F2:home-history-engine', phase: 'Skeleton', effort: 'high' }),
])
const [f3a, f2] = skeleton
log('skeleton done')

phase('Panes')
const panes = await parallel([
  () => agent(`${COMMON}
CONSOLE SKELETON REPORT (context API, stub files you replace):
${f3a}
Also read app/runs/[id]/sections/README.md and workspace-context.tsx before writing code.

YOUR TASK (F3b, findings + finding inspector + decisions). You own: app/runs/[id]/sections/findings-tab.tsx, sections/finding-inspector.tsx, sections/decisions-tab.tsx, plus new sub-components under app/runs/[id]/sections/finding/*.tsx (evidence-table, records-table, ai-envelope, grounding-gates, redteam-panel, proposal-card, abstention-card, deterministic-card).
1. 发现 tab: dense DataTable of report.findings — ID (mono), 标题 (title_zh/en), 列, 风险 (coloured dot + text), 授权模式 (code chip with zh tooltip), 记录, 单元格, 证据 (glyph row ✓ ✗ – from evidence_signals with signal names on hover), 提议来源 (AI violet mark / 确定性 grey / 无资格 neutral, derived from finding.proposal.provider and authorization_mode), 处置 (from run.decisions and dry_run.finding_dispositions). Row click sets selectedFindingId; keyboard up/down moves selection; group headers or a filter for 策略可自动授权 / 需人工决策 / 仅允许隔离. Summary strip above the table: counts by risk and by authorization mode, blocking count.
2. finding-inspector.tsx (right pane when a finding is selected): header (id, title, risk, authorization mode, action, counts, column), sections: 证据 (evidence signals table: signal label, status glyph, explanation_zh/en, evidence_ref mono), 受影响记录 (getFindingRecords → masked DataTable; note which columns are masked), AI 评估 = the envelope (only when finding.proposal exists): three stacked panes — 发送 (the ledger record for this finding's semantic call: find it in getLedger() by finding_id/task; show request_payload verbatim as collapsible mono JSON, request_bytes as 'UTF-8 字节', input_hash HashChip, line '原始记录 0 行 · 已遮蔽字段 N' from redaction summary), 返回 (response_payload mono JSON; model_served, prompt_version, latency_ms, tokens in/out, status pill; served-from-cache label when status is cached), 接地校验 (every grounding reason code from lib/labels.ts as a row: green when absent, red with the offending detail when present; 引擎重算记录数 = grounding.affected_record_count vs '模型未提供记录数'); then proposal-card (mapping table source → target with counts from details.observed_counts when available) or abstention-card (abstain_reason) — and when finding.proposal is null or provider is deterministic, deterministic-card explaining the fallback (reason from the ledger status or events). Buttons: 重新评估 (rerunSemantic, disabled when APPLIED) and the 红队 panel: select case (labels in zh with the English code), button 注入; result renders 模拟篡改 label, a two-column JSON diff (original vs tampered, changed keys highlighted), the grounding gates for the tampered proposal, and a banner 提议已拒绝 · 未生成任何动作 · 发布状态不变 when invalid; LIVE_INJECTION and TIMEOUT get their explanatory captions. Everything reads from real API responses.
3. 处置 tab: for every finding that is not POLICY_AUTHORIZED: a row with title, risk, authorization mode, allowed outcomes as segmented buttons (disallowed outcomes shown disabled with the policy reason, e.g. 仅允许隔离 / AI 提议未通过接地校验), reason chips with preset Chinese text (证据支持受限范围 / 需人工复核 / 敏感字段整列排除 / 业务口径待确认 / 重复导入无需保留) plus free text; policy-authorized findings listed read-only with their authorization_ref preview; unresolved counter; 保存处置 → putDecisions (bulk) → show unresolved from the response; 生成变更集 → createDryRun (409 → guard row with code/message and the unresolved list). Existing decisions prefill the form. A stale dry run (decisions changed after dry-run) is signalled by the context/dry_run absence — show the hint.
Verify: tsc, lint, build.`,
    { label: 'F3b:findings-inspector-decisions', phase: 'Panes', effort: 'high' }),

  () => agent(`${COMMON}
CONSOLE SKELETON REPORT (context API, stub files you replace):
${f3a}
Also read app/runs/[id]/sections/README.md and workspace-context.tsx before writing code.

YOUR TASK (F3c, change set + validation/release). You own: app/runs/[id]/sections/changeset-tab.tsx, sections/release-tab.tsx, plus sub-components under app/runs/[id]/sections/release/*.tsx (action-table, reconciliation, change-preview, validation-table, manifest-card, hash-chain, local-verify, brief-panel, tamper-test-panel, downloads).
1. 变更集 tab: needs run.dry_run (else locked state explaining 先完成处置). Action DataTable (action_type label, finding id, 授权来源 POLICY/HUMAN pill, authorization_ref mono, 范围 = record count or column); reconciliation line with live arithmetic '总记录 N = 可发布 a + 隔离 b + 排除 c (+ 标记 d)' using dry_run counts and report.profile.record_count; change preview DataTable from run.preview (display_key, 列, 修改前 → 修改后 in mono, 发现, action) with the truncated notice; hashes: approved_action_set_hash and decision_set_hash as HashChips; inline confirm block (no modal): shows source hash, action set hash, counts, an auto-generated idempotency key (crypto.randomUUID) and the button 应用并验证 → applyRun; 409 → guard row with code + observed/expected hashes side by side (STALE_DRY_RUN / SOURCE_ARTIFACT_CHANGED / ACTION_SET_CHANGED / VALIDATION_FAILED). Show X-Idempotent-Replay when the response was a replay (from the api-log entry or the api client result).
2. 验证与发布 tab: needs run.execution (else locked). Validation DataTable (check_id mono + zh label, 结果 glyph, observed | expected side by side in mono — hashes as HashChips with equality underline, failing rows show full values), baseline vs candidate metric tiles side by side, manifest card (release status pill, eligible/quarantined/excluded/flagged counts, excluded columns, ai_call_count/provider, contract hash) and the hash chain (source → contract → action set → decisions → candidate → release → change ledger) as HashChips; downloads (release.csv, candidate.csv, release-manifest.json, changes.jsonl, ai-ledger.jsonl, audit-bundle.json via artifactUrl); 本地复验: fetch release.csv (and optionally manifest) as ArrayBuffer → sha256Hex → compare to release_artifact_hash → show both hex strings with the equality underline and 哈希一致 / 不一致; 篡改测试 panel (labelled 演示 · 内存中进行 · 不落盘): button → tamperTest → renders the returned validations with SOURCE_IMMUTABLE red and both hashes, release status BLOCKED, and the caption 未写入任何工件；真实运行未受影响; AI 发布简报 panel: getBrief (poll while pending) → summary_zh/en with ProvenanceMark, claims list where verified=false is struck through with the reason, verified_count/total_count; if brief status is failed or provider deterministic, say so plainly.
Verify: tsc, lint, build.`,
    { label: 'F3c:changeset-release', phase: 'Panes', effort: 'high' }),

  () => agent(`${COMMON}
CONSOLE SKELETON REPORT:
${f3a}

YOUR TASK (F5, offline replay restyle). You own: app/demo/clinical-nlp/page.tsx, app/demo/clinical-nlp/demo-experience.tsx (rewrite), app/demo/clinical-nlp/*.tsx (new helpers), public/manifest.webmanifest (update names only if needed).
The replay is the offline fallback only. Rewrite it as a read-only, clearly labelled 离线回放 · 非实时 view built from the SAME shared components (components/datapilot) and the same visual language as the console, fed by the static golden artifacts in public/demo/ (report.json, release-report.json, release-manifest.json, events.json, cleaned.csv). Note: the backend agents are regenerating those files against the new v0.2 shapes (RunReport with column_profiles, generic finding ids like SEM-diagnosis_label, bilingual titles); code against lib/types.ts, and make the page tolerant of missing optional fields (render 不可用 rather than crash) since the regenerated files may land after you finish. Remove all setTimeout replay animation, the 跳过回放 button, sessionStorage stage persistence, hardcoded zh title maps, the 'RULE NORM-014 CREATED' string, and the Sparkles glossary card. Structure: header strip (same as console but with a persistent 离线回放 badge and a sentence stating this is a recorded engine run, not a live model call), a tab set 概览 / 发现 / 变更集 / 验证与发布 rendering the static report/execution with the shared tables (findings table, evidence, action table, validation table, manifest card, downloads of the static files). The recorded events render as a static log with their real messages (no timers). Language toggle works. Keep the route and the service worker OUT (F2 removes SW registration).
Verify: tsc, lint, build.`,
    { label: 'F5:replay-restyle', phase: 'Panes', effort: 'medium' }),
])
log('panes done')

phase('Gate')
const gate = await agent(`${COMMON}
REPORTS FROM ALL FRONTEND AGENTS:
F2: ${f2}
F3a: ${f3a}
F3b: ${panes[0]}
F3c: ${panes[1]}
F5: ${panes[2]}

YOUR TASK (F6, frontend gate). You own every file under app/, lib/, components/, public/ for this phase (no other frontend agent is running now; backend agents may still be editing services/, tests/, scripts/, fixtures/, docs/ — do not touch those).
1. Make npx tsc --noEmit -p tsconfig.json, npm run lint, and npm run build all pass. Fix real problems; do not silence with any/ts-ignore.
2. Consistency pass across the console: every tab/pane uses the shared components; no leftover stubs ('由其他模块提供'); no hardcoded finding-id strings; every AI element has ProvenanceMark; every hash is a HashChip; 409/422 handling consistent; Chinese primary everywhere; no emoji/exclamation marks/marketing copy (grep for '！', '!', 'Sparkles', 'Verified demo', '全程可解释', '18 fields', 'NORM-014').
3. Dead code: delete components/ui/*.tsx files that nothing imports (grep from app/, components/, lib/ — keep anything referenced), and remove unused hooks/ files; confirm the build still passes after deletion.
4. Static review of the SSE + polling fallback and of the api-log store for memory leaks (unsubscribe on unmount, bounded entries).
5. Start the dev server on a free port (e.g. 3010) and load /, /runs, /engine, /kit, /demo/clinical-nlp, /runs/does-not-exist with playwright-core (channel 'chrome', headless). The v0.2 API is not running, so live pages will show the API-unavailable guard states — that is expected; assert no uncaught exceptions in the console and take screenshots into .artifacts/frontend/*.png. Stop the server.
Report: gate results, deletions, remaining gaps for the integration phase (which needs the real backend).`,
  { label: 'F6:gate', phase: 'Gate', effort: 'high' })

return { f2, f3a, panes, gate }
